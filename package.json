{
	"scripts": {
		"precommit": "npm run bootstrap && npm run lint && npm run test",
		"clean": "lerna run clean",
		"bootstrap": "lerna bootstrap",
		"lint": "lerna run lint",
		"test": "lerna run test"
	},
	"devDependencies": {
		"husky": "^0.14.3",
		"lerna": "^2.4.0"
	}
}
